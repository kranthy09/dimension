# Production Environment Variables Template
# Copy this file to .env and fill in your actual values

# Domain Configuration
DOMAIN=yourdomain.com

# Database Configuration
POSTGRES_DB=portfolio_prod
POSTGRES_USER=portfolio_user
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# Backend Configuration
DATABASE_URL=postgresql://portfolio_user:CHANGE_THIS_TO_STRONG_PASSWORD@db:5432/portfolio_prod
SECRET_KEY=GENERATE_NEW_KEY_WITH_python3_-c_"import_secrets;_print(secrets.token_hex(32))"

# Instructions:
# 1. Generate a strong SECRET_KEY: python3 -c "import secrets; print(secrets.token_hex(32))"
# 2. Generate a strong POSTGRES_PASSWORD: openssl rand -base64 32
# 3. Replace DOMAIN with your actual domain name
# 4. Save this as .env (not .env.production.example)
