# Local Development Environment Variables
# Copy this to .env for local development with docker-compose.yml

# Database Configuration (for local development)
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Backend Configuration
DATABASE_URL=postgresql://:@:/
SECRET_KEY=GENERATE_NEW_KEY_WITH_python3_-c_"import_secrets;_print(secrets.token_hex(32))"

# Instructions for Local Development:
# 1. Copy this file: cp .env.local.example .env
# 2. Adjust credentials if needed
# 3. Run: docker compose up -d
# 4. Create admin user: docker compose exec backend python3 scripts/create_admin.py --email admin@local.dev --password admin123 --name "Admin"
